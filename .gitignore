*.iml
.gradle
/local.properties
# Android Studio の個人設定は共有しない（環境ごとに変わり、差分ノイズになるため）
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# 署名鍵とそのパスワードは絶対にコミットしない（公開リポジトリのため特に重要）
keystore.properties
*.jks
*.keystore

# ビルド成果物。配布用APKは docs/submission-ultra.apk に手動でコピーして管理する。
/app/release

# デザインの指針。サイトを組むときの手元の参照で、配信物ではない。
/docs/DESIGN.md
