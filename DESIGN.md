# 提出物Ultra デザイン指針

このアプリの見た目と手ざわりを決めるための文書。迷ったらここに照らして決める。

外部のデザインシステムは導入しない。Material 3 のコンポーネント（`Scaffold` `Button` `TopAppBar` `NavigationBar` など）は土台として使うが、
何をどれだけ強調するかはこの文書が決める。

> `docs/DESIGN.md` はプロジェクトサイト（`docs/index.html`）を組むための手元の参照で、追跡対象外（`.gitignore`）。
> アプリ本体の指針はこちらだけ。

---

## 一文

**数字と言葉だけで伝える。装飾は主役を消さない。**

このアプリがユーザーに渡しているものは「あと何分で始めないと間に合わないか」という一つの数字である。
それ以外のすべては、その数字を読みやすくするために存在する。

---

## 原則

以下はどれも新しく決めたものではなく、既にコードに書かれていたものを言葉にしたにすぎない。出典を併記する。

### 1. 主役は数字。装飾は主役を消さない

> ボタンは画面幅いっぱいには広げない。押し間違えない大きさを保ちつつ、数字を主役に残す。
> — `ui/alarm/EmergencyAlarmActivity.kt` の `ActionButtonWidthFraction`

> 動かさない。流れると視線がそちらに移り、カウントダウンと競合する。
> — `ui/alarm/EmergencyAlarmActivity.kt` の `HazardBar`

情報を持たない要素が、情報を持つ要素より目立ってはならない。

### 2. 優先度は大きさ・太さ・並び順で表す。色ではない

> 提出物の緊急度は色分けしない（読みやすさを優先し、大きさ・太さ・並び順で表す）。
> — `ui/theme/StatusColors.kt`

> 緊急度による色分けはせず、文字の大きさ・太さだけで優先度を表す。
> — `ui/components/AssignmentCard.kt` の `AssignmentCard`

**ただし、大きさは輝度に裏打ちされて初めて効く。**

最初に目に入るものを決めているのは、背景との**輝度差**とその**面積**である。輝度で負けていれば、
どれだけ大きくしても視線は先に行かない。緊急アラーム画面では、80sp まで拡大したカウントダウンが
背景比 6.6:1 で、補助ラベルの 7.3:1 より暗く、画面上の文字で最下位だった。真上にあった
ハザードテープの白い縞は 16.8:1 で面積も 1.6 倍あり、視線はそこで止まっていた。

**「読める」と「目に入る」は別の指標である。** WCAG は可読性の基準で、大きな文字なら 3:1 で合格する。
それを満たしても誘目性が確保されるわけではない。主役に据えた要素は、**画面上で最も背景比が高い**
ことを実際に計算して確かめる。目視で「目立つ気がする」は当てにならない。

### 3. 色は「見落とすと実害がある」ときだけ使う

> 色を使うのは「緊急通知が鳴るか鳴らないか」のように、見落とすと実害がある状態表示だけ。
> — `ui/theme/StatusColors.kt`

色は有限の予算である。日常の状態表示に使うと、本当に危ないときに使う色が残らない。

そして色を使う場合も、**色だけを手がかりにしない**。必ず形・アイコン・ラベルのいずれかを併用する。

### 4. 意味は端末の設定で変わらない

> 動的カラー（Material You）は使わない。端末ごとに色が変わると、状態色（緑/橙/赤）の意味が読み取りにくくなるため、固定の配色で見やすさを担保する。
> — `ui/theme/Theme.kt`

同じ理由で、ユーザーの文字サイズ設定は**尊重する**。大きくしても壊れない組み方をする。

> 端末の文字サイズ設定（fontScale）も勘定に入れないと、大きく設定した人だけ溢れる。
> — `ui/alarm/EmergencyAlarmActivity.kt` の `Countdown`

### 5. 面の階層は影ではなく面色で表す

> 面の階層（背景 < カード）をはっきり分け、文字は十分なコントラストを確保する。
> 背景をわずかに色味のあるグレーにし、カード(白)が浮いて見えるようにする。
> — `ui/theme/Color.kt`

影は補助。面色と枠線で階層が読めていることを先に成立させる。

### 6. 動かさない。明滅させない

> 明滅（点滅）にはしない。画面の縁は面積が大きく、高コントラストで点滅させるのは光過敏性発作のリスクがある。
> — `ui/alarm/EmergencyAlarmActivity.kt` の色定数まわりのコメント

動きを使ってよいのは、**状態の変化を伝えるとき**だけ。装飾目的では使わない。
警告・エラー・破壊的操作には、跳ねる（オーバーシュートする）動きを使わない。

**幾何に依存する装飾を置かない。** 画面の縁を囲む枠のような表現は、端末ごとに違う角丸半径と
必ず衝突する。緊急アラーム画面にはかつて脈動する赤い枠があったが、角丸のある端末で四隅が
切り落とされること、そして「秒針として働く」という役目をカウントダウンのミリ秒表示が既に
果たしていたことの 2 点から廃止した。

### 7. 見本は実物を呼ぶ。複製しない

> 見本は実物の `AssignmentCard` をそのまま使う。作り物の絵を置くと本番の画面とずれていくが、本物を置けば必ず一致する。
> — `ui/onboarding/OnboardingScreen.kt`

チュートリアルやプレビューで画面の一部を見せるときは、その画面が使っている composable をそのまま呼ぶ。

### 8. テストは本番と同一経路を通る

> テスト通知は本番と同じ関数・同じチャンネルを通る。ここで鳴ることが、本番でこの端末設定でも鳴ることの証明になる。
> — `ui/settings/TestNotificationScreen.kt`

「確認用の表示」を別実装で用意しない。それは確認になっていない。

### 9. 緊急時は逃げ道を作らない

> この画面は逃げ道を作らない
> — `ui/alarm/EmergencyAlarmActivity.kt` の `EmergencyAlarmActivity`

ただしこれが許されるのは緊急アラーム画面だけ。それ以外の画面で、ユーザーの操作を塞いではならない。

---

## トークン

現在コードで使われている値の分布から起こした。推測値ではない。
定義は `ui/theme/Tokens.kt`。この文書と一対一で対応させる。

### 間隔 `Space`

| 名前 | 値 | 用途 |
|---|---:|---|
| `xs` | 4dp | 行間の詰め、ラベルと値の間 |
| `sm` | 8dp | 要素間の標準 |
| `md` | 12dp | カード内の要素間 |
| `lg` | 16dp | 画面の左右余白、カードの内側 |
| `xl` | 24dp | セクション間 |
| `xxl` | 32dp | 画面上部・下部の大きな余白 |

4の倍数に揃える。中間の値（6, 10, 14, 20, 28dp）は使わず、最寄りに寄せる。

**寸法は間隔ではない。** アイコンの大きさ（88dp）、状態ドットの直径（10dp）、列を揃えるための
幅（48dp）などは、その要素固有の寸法であって間隔ではないので `Space` には入れない。
使う場所で名前付きの定数にして、なぜその値なのかを書く。

### 角丸 `Radius`

| 名前 | 値 | 用途 |
|---|---:|---|
| `sm` | 8dp | 内側の小さい面（バナー、プレビュー枠） |
| `md` | 12dp | カード（標準） |
| `lg` | 16dp | 主役の面（ヒーロー） |

面が大きいほど角丸も大きい。この3つ以外を新しく作らない。

### 枠線 `Stroke`

| 名前 | 値 | 用途 |
|---|---:|---|
| `hairline` | 1dp | 面の境界、区切り線 |

### 面の階層 `SurfaceLevel`

| 名前 | 面色 | 枠線 | 影 | 角丸 |
|---|---|---|---:|---|
| `Base` | `background` | なし | 0dp | `sm` |
| `Raised` | `surface` | hairline / `outlineVariant` | 0dp | `md` |
| `Lead` | `surface` | hairline / `outline` | 2dp | `lg` |

影の値を各所で直書きしない。階層はこの3段階しかない。

通常のカードには影を落とさない。面色と枠線だけで階層が読めていることを先に成立させ、
影は主役の面にだけ使う。ダークモードでは影がほとんど見えないため、影に意味を持たせない。
枠線の濃さ（`outlineVariant` と `outline`）も階層の一部として働く。

---

## 強調の規則

**1画面につき、最も強調された要素は1つ。**

これはこの文書で最も重要な規則であり、最も破られやすい規則でもある。
2つ作りそうになったら、実装する前に「どちらがこの画面の存在理由か」を決める。

### 段階 `Emphasis`

| 名前 | 値 | 意味 |
|---|---:|---|
| `Lead` | 36sp / Bold | **画面の主役。1画面に1つだけ** |
| `Strong` | 22sp / Bold | 主役に次ぐ数字（カード内のカウントダウンなど） |
| `Body` | `Typography` に委ねる | 本文 |
| `Quiet` | `Typography` + `onSurfaceVariant` | 補助情報 |

### 主役の表し方は1つではない

`ScreenLead` は「どれが主役か」の宣言であって、「36spにする」という意味ではない。

- **数字がある画面**（ホーム、編集、完了済み）は `Emphasis.Lead` の大きさで主役を示す。
  このアプリの主題は時間なので、数字が主役になるのが自然な形。
- **数字がない画面**（緊急通知の状態、設定）は、面（`SurfaceLevel`）と位置で主役を示し、
  文字は画面内で最大にするだけに留める。結論が「鳴ります」のような短い言葉のとき、
  それを36spにしても読みやすくはならない。

どちらの場合も、主役は1画面に1つ。

### 宣言のしかた

画面のルートを `EmphasisScope` で包み、主役を `ScreenLead` で宣言する。

```kotlin
EmphasisScope("ホーム") {
    Scaffold(...) {
        ScreenLead {
            // この画面の主役をここに置く
        }
    }
}
```

同じ `EmphasisScope` の中で `ScreenLead` が2つ現れると、**開発ビルドと Preview で例外になる**
（署名済みのリリースビルドでは数えるだけで何もしない）。型では強制できない規則を、
Preview を開いた瞬間に気づける形にするための仕掛け。

数えるのは composition の回数ではなく画面に出ている実体の数なので、再コンポーズでは増えない。
条件分岐で主役を差し替える書き方（片方が消えて片方が現れる）でも誤検知しない。

`EmphasisScope` の外で `ScreenLead` を呼んでも何も起きない。部品単体の Preview を壊さないため。
逆に言うと、**画面のルートを包み忘れると見張りが効かない**。新しい画面を足すときは忘れないこと。

### 例外：主役を置かない画面

- **チュートリアル（`OnboardingScreen`）** — `HorizontalPager` は隣のページも同時に構成するため、
  ページごとに主役を宣言すると 2 つ同時に存在してしまう。この画面は 1 ページに 1 つの話題しか
  無く、そもそも主役を競う相手がいないので、`EmphasisScope` を置かない。
- **緊急アラーム画面（`EmergencyAlarmActivity`）** — 聖域（後述）。画面全体が主役なので宣言しない。

主役が 0 個の画面があってよい。禁じているのは 2 個以上。

### 2つになりそうなときの解き方

優先順に試す。

1. **状況で切り替える。** 両方を同時に最強調にしない。片方が出ているときはもう片方が譲る。
   例: ホームで「緊急通知が鳴りません」の警告が出ている間は、ヒーローカードは主役を譲る。
   通知が鳴らない状態の解消が、その瞬間の最重要事項だから。
2. **どちらかを `Strong` に落とす。** 本当に同格なら、それは片方が主役ではないということ。
3. **画面を分ける。** ここまで来て決まらないなら、1つの画面に2つの目的が入っている。

---

## やらないこと

- 動的カラー（Material You）を使わない
- 提出物の緊急度を色で分けない
- 色だけで意味を伝えない（必ず形・アイコン・ラベルを併用する）
- 明滅させない。装飾目的で動かさない。警告・エラー・破壊的操作に跳ねる動きを使わない
- 破壊的操作（削除）を強調しない。主要アクションと同じ大きさにしない
- 見本を手書きで複製しない（実物の composable を呼ぶ）
- 見た目のために情報アーキテクチャやナビゲーション構造を変えない
- 見た目の変更のついでにロジック・データ層に触れない
- 縦スクロールのリスト、ラベル付きアクション、標準的なナビゲーションを壊さない
  （アイコンのみへの置き換え、リストの装飾的レイアウト化をしない）
- 既存のアクセシビリティ実装（`contentDescription`、スクロール確保、fontScale 対応）を削除・簡略化しない
- 「デザインシステムの推奨だから」を理由に上記のいずれかを正当化しない

---

## 聖域: `EmergencyAlarmActivity`

**この画面だけは、この文書のトークンとテーマの適用対象外とする。**

理由:

- 鳴るのは夜であることが多く、全画面の白は反射的に目をそらさせる。だから固定の暗い面を使う
- 各色の背景比を実測して決めてある。主役のカウントダウンが最上位になるよう並べる:

  | 要素 | 色 | 背景比 |
  |---|---|---:|
  | カウントダウン | `#FFFFFF` | 18.3:1 |
  | 課題名 | `#F2F5F9` | 16.7:1 |
  | ハザードテープの縞 | `#C9CED6` | 11.6:1 |
  | 数字を挟む語（通常時） | `#9AA4B2` | 7.3:1 |
  | 期限超過の語・先生の行・ボタン | `#FF6B60` | 6.6:1 |

- 赤は「状態を表す色」として使う。通常の残り時間は白、期限を過ぎたときだけ前後の語が赤になる
- カウントダウンのフォントサイズは、使える幅と `fontScale` から毎回逆算している（`Countdown`）。
  固定のタイポスケールでは、文字を大きく設定した人だけ数字が溢れる

したがってこの画面は、システムのダーク/ライトに追従させない。色をテーマから引かない。
`Emphasis` のスケールも適用しない。ただし**原則そのもの（1〜9）はここでも守る**。

### 聖域は不可侵ではない

トークンを当てないというだけで、批評の対象外という意味ではない。
実際この画面は、上の原則に照らして 2 つの欠陥が見つかり、直している。

- **幾何に依存する装飾があった** — 画面全体を囲む赤い枠。原則6 に反していた
- **主役が最上段になかった** — 課題名が 28sp で最初に目に入り、残り時間はその下だった。
  叩き起こされて最初に知りたいのは残り時間なので、順序を入れ替えた。原則1 の適用漏れ
- **主役が画面で最も暗い文字だった** — カウントダウンが 6.6:1 で、補助ラベルの 7.3:1 より下。
  大きさだけ与えて輝度を測っていなかった。原則2 の適用漏れ

余白も 176dp から 116dp に詰めた。触るときは実測値と意図を壊していないかを確認する。
ただし「聖域だから触らない」は理由にならない。

---

## この文書の使い方

- 実装で迷ったら、まずここを読む
- ここに書かれていない判断をしたら、この文書に追記する
- 実装とこの文書がずれていることに気づいたら、**どちらかを直す**。放置しない
- 新しい原則を足すときは、なぜそうするかを1行で書く。理由のない規則は守られない
- **出典は行番号で書かない。** ファイル名と関数・定数の名前で書く。
  行番号は import を 1 行足しただけでずれ、しかもずれたことに誰も気づかない。
  この文書は一度それで全引用が無効になっている
